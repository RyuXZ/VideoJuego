// Evento Create de oControladorGlobal (u otro objeto en la primera habitación)
if !(variable_global_exists("global.contador_monedas")) {
    global.contador_monedas = 0; // Inicializa el contador solo si no existe
}
